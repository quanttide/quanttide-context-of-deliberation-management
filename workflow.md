# 议事流程

根据《议事档案标本契约 v0.0.2》的结构定义、生命周期节点和业务预期，我们可以反推出团队当前的议事流程。该流程以“议题”为核心，强调从创建到归档的完整闭环，同时允许根据议题类型跳过某些节点（如附议、决议）。以下是反推得到的标准议事流程：

---

## 议事流程（反推自契约）

### 1. 议题创建
- **触发条件**：需要集体讨论或决策的任何事项。
- **操作**：发起人按议题模式（研讨、提案）选择对应模板，填写核心元数据（如标题、提出人、时间等）。议事规则、议程、决议为独立实体，不走议题流程（规则修订经提案议题产出，议程由会议编排，决议在决议形成节点产出）。
- **对应生命周期节点**：`议题创建`
- **输出**：一个带有模式标记的议题草稿，包含预期字段（如动议区、附议区等占位）。

### 2. 动议提出
- **触发条件**：议题内容需要明确、可执行的具体提议。
- **操作**：在议题的“动议区”填写核心内容。根据议题模式不同，动议区的内容不同：
  - 研讨 → 研讨主题、讨论框架
  - 提案 → 提案内容、选项列表、决策准则
- **对应生命周期节点**：`动议提出`
- **输出**：一个包含完整动议的议题记录。

### 3. 附议扩散（研讨为开放讨论，提案为封闭表态）
- **触发条件**：议题需要其他成员参与。
- **操作**：
  - 研讨模式：附议区承载**开放深度讨论**——多方补充、深度意见、观点碰撞
  - 提案模式：附议区为**封闭表态**——附议/反对/弃权，无开放式讨论
- **对应生命周期节点**：`附议扩散`
- **输出**：附议区被填充（研讨为讨论记录，提案为表态结果）。

### 4. 决议形成（研讨可选，提案必经）
- **触发条件**：议题经过讨论或评估后，需要做出最终决定。
- **操作**：在“决议区”记录最终决定、投票结果或结论。研讨模式中结论可选（可能只沉淀讨论而不拍板）；提案模式中决议为必经环节。
- **决议独立管理**：一旦形成决议，产出的决议实体（内容、责任人、完成期限、执行状态）脱离议题，进入独立的决议档案统一管理——方便执行跟踪、逾期预警与治理审计。
- **对应生命周期节点**：`决议形成`
- **输出**：决议实体（进入决议档案单独管理）

### 5. 归档沉淀
- **触发条件**：议题已完成动议、附议和决议（如有），内容不再变动。
- **操作**：将完整的议题记录存入知识库“议事档案”，按年份+周次组织。标注归档时间，确保可追溯。
- **对应生命周期节点**：`归档沉淀`
- **输出**：一个固化在知识库中的议题标本。

---

## 流程特点总结

- **固定骨架**：五节点生命周期在系统中固定（议题创建 → 动议提出 → 附议扩散 → 决议形成 → 归档沉淀），所有议题共享同一框架，不可变。
- **两类模式**：研讨（开放深度讨论，附议为开放讨论、决议可选）与提案（封闭决策，附议为封闭表态、决议必经）。模式对五环节的要求由契约 `stage_requirements` 配置。
- **模式驱动**：议题模式决定了动议区、附议区和决议区的语义，但所有议题共享同一套生命周期节点。
- **无辩论区**：提案类封闭决策不设开放式讨论，走动议-附议-决议简洁路径；开放深度讨论由研讨类承载，分歧的深度探索也可由议事云的 AI 模拟辩论承担（思考者与行动者角色分别模拟对立论证），作为决策支持。
- **反推验证**：AI 可将实际文档内容与此流程对照，检查缺失或多余的步骤，从而持续优化契约。

---

此流程并非预设模板，而是从已有议事档案中“发现”的结构固化而成，符合契约“模型从资产中长出”的核心理念。
